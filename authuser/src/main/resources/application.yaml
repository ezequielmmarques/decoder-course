server:
  port: 8087

spring:
  datasource:
    url: jdbc:postgresql://localhost:55000/ead-authuser
    username: postgres
    password: postgrespw
  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true


